{{- if .Values.registerDns }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "hmcts.releaseName" . }}-bin
  labels:
    system: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    type: configuration
data:
  feature-toggle-dns: |
{{ include (print .Template.BasePath "/bin/_dns-register.tpl") . | indent 4 }}
{{- end }}